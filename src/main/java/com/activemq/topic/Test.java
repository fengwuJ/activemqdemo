package com.activemq.topic;

import javax.jms.Topic;

public class Test {
    //将Queue queue = session.createQueue("hello1115");
    // 改为：
    //Topic topic = session.createQueue("hello1115");
    //session.createProducer(topic);

    //首先启动消费者，再启动生产者
}
